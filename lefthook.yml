pre-commit:
  parallel: true
  jobs:
    - run: bun lint:fe
      glob: "frontend/**/*"
      stage_fixed: true
    - run: bun lint:be && bun test:be
      glob: "backend/**/*"
      stage_fixed: true
    - run: bun generate:openapi
      glob: "backend/app/controllers/**/*,backend/app/serializers/**/*"
      stage_fixed: true
